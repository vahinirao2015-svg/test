apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: assetledger-data
  namespace: assetledger
spec:
  storageClassName: gp3
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
